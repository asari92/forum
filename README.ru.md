# Web Forum (Go)

📄 [English version](README.md)

Веб-форум, реализованный на Go.  
Проект демонстрирует архитектурный подход к backend-разработке, работу с HTTP, аутентификацию, модерацию и серверный рендеринг без использования тяжёлых фреймворков.

---

## Что демонстрирует проект

Проект отражает практики и архитектурные решения, применяемые в реальных серверных приложениях на Go.

В проекте показано:

- слоистая архитектура (handler, service, repository)
- чёткое разделение ответственности между слоями
- явное управление зависимостями без фреймворков
- серверный рендеринг HTML-шаблонов
- аутентификация и управление сессиями
- OAuth 2.0 (Google, GitHub)
- workflow модерации и одобрения контента
- проектирование БД с внешними ключами
- HTTPS и базовая безопасность
- контейнеризация с помощью Docker

---

## Основные возможности

- регистрация и аутентификация пользователей
- вход через Google и GitHub
- роли: пользователь, модератор, администратор
- создание постов с категориями и изображениями
- древовидные комментарии
- реакции (лайки и дизлайки)
- модерация и одобрение постов
- система уведомлений
- защита сессий и CSRF

---

## Архитектура

Приложение построено по слоистой архитектуре:

- Handler-слой отвечает за HTTP, роутинг и ответы
- Service-слой содержит бизнес-логику и сценарии использования
- Repository-слой инкапсулирует работу с базой данных
- Entities описывают доменные модели
- UI-слой включает HTML-шаблоны и статические ресурсы, встроенные в бинарник

Такой подход упрощает тестирование, поддержку и развитие проекта.

---

## Структура проекта

```
cmd/          – точка входа приложения
internal/     – основная логика приложения
  handler/    – HTTP-обработчики и роутинг
  service/    – бизнес-логика
  repository/ – доступ к данным
  entities/   – доменные модели
  session/    – управление сессиями
pkg/          – переиспользуемые пакеты и конфигурация
ui/           – HTML-шаблоны и статика
schema/       – схема базы данных
```

---

## Технологии

- Go 1.22
- SQLite
- net/http
- html/template
- OAuth 2.0
- Docker
- TLS (HTTPS)

---

## Запуск локально

### Требования

- Go 1.22+
- Git
- SQLite
- OpenSSL

### Установка и запуск

Клонировать репозиторий:

```bash
git clone https://github.com/asari92/forum
cd forum
```

Инициализировать базу данных и TLS-сертификаты:

```bash
make init
```

Запустить сервер:

```bash
go run ./cmd/web/main.go
```

Приложение будет доступно по адресу:

https://localhost:4000

---

## Запуск через Docker

Сборка образа:

```bash
make build
```

Запуск контейнера:

```bash
make run
```

Остановка:

```bash
make stop
```

---

## Примечания

Выбор технологий сделан осознанно, чтобы подчеркнуть базовые принципы Go: явное управление зависимостями, чёткое разделение ответственности и предсказуемые потоки работы с HTTP и данными.
